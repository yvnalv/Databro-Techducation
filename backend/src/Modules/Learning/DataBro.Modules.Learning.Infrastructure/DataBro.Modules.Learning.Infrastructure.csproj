<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\DataBro.Modules.Learning.Application\DataBro.Modules.Learning.Application.csproj" />
    <ProjectReference Include="..\..\..\Shared\DataBro.Platform.Persistence\DataBro.Platform.Persistence.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="9.0.18">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="9.0.4" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Core only: the module declares its recurring sweep, and the host owns the server and
         storage (docs/ARCHITECTURE.md). -->
    <PackageReference Include="Hangfire.Core" Version="1.8.20" />
  </ItemGroup>

</Project>
