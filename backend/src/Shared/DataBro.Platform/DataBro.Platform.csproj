<Project Sdk="Microsoft.NET.Sdk">

  <!-- Domain-safe shared kernel. Intentionally has NO external dependencies so that
       Domain layers (which reference Platform) stay free of framework/persistence concerns.
       EF Core lives in DataBro.Platform.Persistence, referenced only by Infrastructure. -->

</Project>
